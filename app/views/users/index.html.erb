<%= link_to  I18n.t('user.new.title'), new_user_path, :class => "button confirm" %>

<h1><%= I18n.t('user.index.title')%></h1>

<table>
  <tr>
    <th><%= I18n.t('user.l.name')%></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= link_to I18n.t('show'), user %></td>
    <td><%= link_to I18n.t('edit'), edit_user_path(user) %></td>
    <td><%= link_to I18n.t('destroy'), user, confirm: I18n.t('are_you_sure'), method: :delete %></td>
  </tr>
<% end %>
</table>

<br />


