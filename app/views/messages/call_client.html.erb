<% if flash[:notice] %>
<p class="notice"><%= flash[:notice]%></p>
<% end %>
<% if flash[:error] %>
<p class="error"><%= flash[:error]%></p>
<% end %>


<%= form_tag({:action =>'docall_client'}) do |f| %>
<%= label_tag I18n.t('phonenumber') %>
<%= telephone_field_tag :phonenumber %>
  <% if not @message.nil? and @message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@message.errors.count, "error") %> prohibited this plivo from being saved:</h2>

      <ul>
      <% @message.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= hidden_field :message, :name, :value => "docall" %>
  <%= hidden_field :message, :id, :value => @message.id %>
  <div class="actions">
    <%= submit_tag I18n.t('test')%>
  </div>
<% end %>
